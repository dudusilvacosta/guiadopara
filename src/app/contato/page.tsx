import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Guia do Pará | Contato",
  description: "Criado por Eduardo Silva Costa",
};

export default async function ContatoPage() {
  return <h2>Em desenvolvimento...</h2>;
}
